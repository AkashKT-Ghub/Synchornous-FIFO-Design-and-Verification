# Synchornous-FIFO-Design-and-Verification
A Synchronous FIFO is a queue that stores data in the order it was received and retrieves it in the same order. It is called synchronous because both the read and write operations use the same clock signal.
